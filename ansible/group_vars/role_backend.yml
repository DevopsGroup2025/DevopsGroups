# Variables for backend servers (private subnet)
ansible_user: ubuntu
ansible_ssh_private_key_file: terraform/bastion-key-devops-notes-app.pem

# Use load balancer as jump host
ansible_ssh_common_args: >-
  -o ProxyJump=ec2-user@{{ hostvars[groups['role_load_balancer'][0]].ansible_host }} #magic___^_^___line
# Backend application variables
app_dir: "/home/{{ ansible_user }}/backend"
backend_port: 3001

# Database configuration
database_username: dbadmin
database_password: password
database_name: notesdb

# ECR Docker settings
ecr_repository: "backend-app-repo"
ecr_region: "eu-west-1"
# Add ecr_account_id from Terraform/account_id
# This is the AWS account ID, which can be extracted from the ECR URL or set as a variable
# The build-and-deploy.sh script already extracts ACCOUNT_ID, so we can pass it as an extra var
# For now, set it statically to match your AWS account (414392949441)
ecr_account_id: "414392949441"
docker_image_tag: "latest"
